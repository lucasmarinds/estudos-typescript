/** O Non-Null assertion, é uma maneira de validarmos um valor que não recebmos ainda igual exemplo abaixo
 * que nao precisamos validar se ele tem valor ou não, isso já é feito com o operador "!",
 * diferente do "?" que é para dizer que o elemento é opcional ou nao.
 */
var p = document.getElementById("some-p");
console.log(p.innerText);
