/**
 * Criar uma função que aceite dois argumentos somente tipo number.
 * e exibir a soma dos dois.
 */
let numero = 10;
let numeroDois = 20;
function soma(x:number, y:number){
    return console.log(x + y);
}
soma(numero, numeroDois);
